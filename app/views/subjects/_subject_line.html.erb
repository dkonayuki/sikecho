<div class='subject-item-line' %>
	<%= link_to subject, class: "subject-thumbnail-line" do %>
  		<%= image_tag(subject.display_picture_small) %>
  <% end %> 
	
  <div class="subject-info-line">
  	<div class="subject-year-line">
			<%= subject.year %>年
		</div>
		
		<div class="subject-course-line">
    	<%= subject.semester.uni_year.name %><%= subject.semester.name %>
  	</div>		
	</div>
	
	<% if user_signed_in? %>
	<div class="subject-menu-line" data-name="<%= subject.name %>" data-img="<%= image_path(subject.display_picture_small) %>">
		<% if current_user.current_subjects.include?(subject) %>
  		<%= link_to schedule_destroy_path(subject: subject.id), method: :delete, class: 'btn btn-sm btn-red subject-menu-line-hover' do %>
    		<span class="btn-text registered-text">登録済み</span>
    		<span class="btn-text cancel-text"><span class="glyphicon glyphicon-remove-sign"></span>受講解除</span>
    	<% end %>
  	<% else %>
    	<%= link_to schedule_create_path(subject: subject.id), method: :post, class: 'btn btn-sm btn-navi subject-menu-line-register' do %>
    		<span class="glyphicon glyphicon-plus-sign"></span>受講登録
    	<% end %>
  	<% end %>
	</div>
	<% end %>
	
  <div class="subject-details-line">
    <%= link_to subject, class: "subject-name-line" do %>
    	<%= subject.name %>
    <% end %>
		<div class="subject-teacher-line">
    	<% subject.teachers.each do |teacher| %>
    		<% if teacher != subject.teachers.first %>
    		・
    		<% end %>
    		<%= teacher.name_kanji %>
  		<% end %>
    </div>
		<div class="subject-desc-line">
			<%= strip_tags(subject.description) %>
		</div>
  </div>
  
</div>