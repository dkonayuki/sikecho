<% if @show_more %>
	$("#subjects-list #loading").remove();
	$('#subjects-list').append("<%=j render @subjects %>");
	<% sleep 1 %>
<% else %>
	$('#subjects-list').html("<%=j render(@subjects) || '授業が見つかりません。' %>");
<% end %>
	$(".hidden-pagination").html("<%=j paginate @subjects %>")
