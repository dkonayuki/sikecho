<% if @show_more %>
	$("#notes-list #loading").remove();
	$('#notes-list').append("<%=j render @notes %>")
	<% sleep 1 %>
<% else %>
	$('#notes-list').html("<%=j render(@notes) || 'ノートが見つかりません。' %>")
<% end %>
	$(".hidden-pagination").html("<%=j paginate @notes %>")
